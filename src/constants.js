// This is a file that will contain all the constants variables that we can use
// We can also define the variables in the .env file but it depends on us as they are not so sensitive

// Database name
export const DB_NAME = "coderstube"